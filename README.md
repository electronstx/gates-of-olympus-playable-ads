# Gates of Olympus | High-Performance Playable Ad

Профессиональный рекламный креатив (Playable Ad) для мобильных платформ, разработанный с нуля на **Vanilla TypeScript** и **Canvas 2D API**. Проект оптимизирован под требования площадки **Moloco** и стандарты Facebook Playable Ads.

## ⚡️ Основные характеристики

- **Вес бандла:** ~1.5 МБ (включая графику и звук в Base64).
- **Производительность:** Стабильные 60 FPS на мобильных устройствах.
- **Технологии:** TypeScript, Canvas 2D, Vite.
- **Целевая платформа:** Moloco, Facebook.

---

## 🛠 Технический стек и особенности

### 🏗 Архитектура (Custom MVC)

Проект реализован на модульной архитектуре, что позволяет легко изменять логику без переписывания рендерера:

- **Core Engine:** Рендерер с поддержкой HiDPI/DPR, асинхронный загрузчик ассетов и высокоточный Тикер на базе `requestAnimationFrame` с расчетом Delta Time.
- **Scripted Scenarios:** Система предсказуемых спинов. Каждый из шагов игрока четко прописан в конфигурации (комбинации, суммы выигрыша, типы визуальных эффектов).
- **Memory Management:** Реализован метод `destroy()` для всех сущностей, очистка слушателей событий `window` и остановка всех таймеров для предотвращения утечек памяти.

### 💎 Визуальные эффекты и рендеринг

- **Cascading Reels:** Реалистичная анимация барабанов с использованием Clipping Mask. Реализована сложная физика: "улет" старых символов вниз и падение новых.
- **3D Particle System:** Дождь из монет. Иллюзия 3D-вращения реализована через динамическое изменение горизонтального масштаба (Math.cos) без использования WebGL.
- **Adaptive Layout:** Двухточечная система привязки (Anchors). Элементы интерфейса динамически распределяются между верхним и нижним краем экрана, обеспечивая идеальный вид на iPhone (9:19) и iPad (3:4).
- **Object Pooling:** Монеты и символы переиспользуются через систему пулов, что исключает лаги от работы Garbage Collector.

### 🔊 Аудио-система

- Кастомный `AudioManager` с предзагрузкой (Preload) и кэшированием аудио-буферов.

### 📦 Сборка

Проект собирается в **единый HTML-файл**, соответствующий требованиям Moloco Ads.

---

## 📁 Структура проекта

````text
src/
├── assets/          # Исходные изображения (WebP) и аудио (MP3)
├── scripts/
│   ├── config/      # Атлас спрайтов, сценарии спинов и общие настройки
│   ├── core/        # Ядро: Renderer, Ticker, Scene, Audio
│   └── game-objects/    # Игровые объекты: Reels, Symbol, Zeus, WinPopup, Coin и т.д.
└── main.ts      # Точка входа и инициализация игры

---

## 🚀 Запуск и Сборка

**Установка зависимостей:**
```bash
pnpm install
````

**Запуск режима разработки (с Hot Reload):**

```bash
pnpm dev
```

**Сборка финального бандла для Moloco:**

```bash
pnpm build
```
